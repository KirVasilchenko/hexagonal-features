group 'com.github.kirvasilchenko.hexagon.app.payment.core.port.output'

configureJavaModule(project)

dependencies {
    implementation project(':app:payment:core:domain')
    implementation project(':app:user:core:domain')
}